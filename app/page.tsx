"use client";

import React, { useState } from "react";
import { FaMoon } from "react-icons/fa6";
import { IoMdSunny } from "react-icons/io";
import { useSelector } from "react-redux";

const page = () => {
  return (
    <div className={`min-h-[100vh-90px]`}>
      <main>Where we start</main>
    </div>
  );
};

export default page;
